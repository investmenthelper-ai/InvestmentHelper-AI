{
  "compilerOptions": {

    "target": "ESNext",                                 /* Use modern JavaScript features. */
    "module": "ESNext",                                 /* Use ES modules for modern bundlers like Vite. */
    "jsx": "react-jsx",                                 /* Use the modern JSX runtime for React 17+. */
    "lib": ["DOM", "DOM.Iterable", "ESNext"],           /* Include browser and modern JavaScript APIs. */

    /* Module Resolution */
    "moduleResolution": "node",                        /* Ensure compatibility with Node.js module resolution. */
    "baseUrl": "./",                                    /* Set the base URL for imports. */
    "paths": {                                          /* Define path aliases if needed. */
      "@/*": ["src/*"]
    },
    "resolveJsonModule": true,                          /* Allow importing .json files. */

    /* Strictness and Type Checking */
    "strict": true,                                     /* Enable all strict type-checking options. */
    "esModuleInterop": true,                            /* Allow compatibility with CommonJS modules. */
    "forceConsistentCasingInFileNames": true,           /* Enforce consistent casing in imports. */
    "skipLibCheck": true,                               /* Skip type checks for declaration files. */

    /* Debugging and Emit */
    "sourceMap": true,                                  /* Enable source maps for debugging. */
    "outDir": "dist",                                   /* Specify the output directory. */
    "rootDir": "src",                                   /* Specify the root directory of your source files. */

    /* Experimental */
    "experimentalDecorators": true,                    /* Enable support for decorators. */
    "emitDecoratorMetadata": true                      /* Emit metadata for decorators. */
  },
  "include": ["src"],                                   /* Include all files in the src directory. */
  "exclude": ["node_modules", "dist"]                  /* Exclude unnecessary files from compilation. */
}
